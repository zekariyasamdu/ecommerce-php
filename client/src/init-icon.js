import { createIcons, Heart, House, Menu, Search, User } from "lucide";

export function initIcons() {
  createIcons({
    icons: {
      Menu,
      Search,
      User,
      Heart,
      House,
    },
  });
}
