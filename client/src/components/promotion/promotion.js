import "./promotion.css";

const Promotion = {
  render: async () => {
    return `
      <div class="promotion-card"> 
      </div>
      `;
  },
  after_render: async () => {},
};
export default Promotion;
